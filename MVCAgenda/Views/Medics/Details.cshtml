
@{
    ViewData["Title"] = "Details";
}


@if (User.Identity.IsAuthenticated)
{

    @model MVCAgenda.Core.ViewModels.MedicViewModel

    <h1>Detalii</h1>

    <div>
        <h4>Medic</h4>
        <hr />
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="Hidden" />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.MedicName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.MedicName)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model.Id">Modifica</a> |
        <a asp-action="Manage" asp-controller="Home">Inapoi</a>
    </div>
}
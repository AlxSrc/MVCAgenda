
@{
    ViewData["Title"] = "Details";
}


@if (User.Identity.IsAuthenticated)
{

    @model MVCAgenda.Domain.Camera

    <h1>Detalii</h1>

    <div>
        <h4>Camera</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.DenumireCamera)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.DenumireCamera)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model.CameraId">Modifica</a> |
        <a asp-action="Manage" asp-controller="Home">Inapoi</a>
    </div>
}